{
  "name": "release-radar",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts",
    "db:migrate:postgres": "psql \"$DATABASE_URL\" -f migrations/0001_postgres.sql"
  },
  "devDependencies": {
    "tsx": "^4.23.13",
    "typescript": "^5.8.3",
    "wrangler": "^4.102.0"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
