# Calendar UX

Calendar subscription setup belongs in the global Calendar area (`/calendar` and the protected `/admin/calendar`). Content detail and picker pages should only expose content actions such as **Add to calendar**, **Manage calendar**, **Add**, and **Remove**. Do not repeat Calendar setup links on individual series/movie pages or inside the series/movie pickers.
